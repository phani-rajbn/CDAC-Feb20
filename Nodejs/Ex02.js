//U could write all UR JS code here....
add = function(first, second){
    return first + second;
}

sub = function(first, second){
    return first - second;
}

remainder = function(first, second){
    return first % second;
}

